PROJECT(my_v4l2)

AUX_SOURCE_DIRECTORY(. SRC_LIST)

ADD_LIBRARY(my_v4l2 SHARED ${SRC_LIST})
SET_TARGET_PROPERTIES(my_v4l2 PROPERTIES VERSION ${VERSION} SOVERSION ${SOVERSION})

INSTALL(TARGETS my_v4l2
		LIBRARY DESTINATION ${SUYUN_SDK_DIR}/lib)

