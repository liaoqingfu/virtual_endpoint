PROJECT(ffmpeg_lib)

AUX_SOURCE_DIRECTORY(. SRC_LIST)

ADD_LIBRARY(ffmpeg_lib SHARED ${SRC_LIST})
SET_TARGET_PROPERTIES(ffmpeg_lib PROPERTIES VERSION ${VERSION} SOVERSION ${SOVERSION})

INSTALL(TARGETS ffmpeg_lib LIBRARY DESTINATION ${SUYUN_SDK_DIR}/lib)

#end confirm ACE
